// The array we want to destructure
let apps = ["Instagram", "YouTube", "LinkedIn"];
{
// Use array destructuring to assign the first two elements to variables
const [apps1, apps2] = apps;

// Print the results
console.log(`The first app is: ${apps1}`);
console.log(`The second app is: ${apps2}`);
}
