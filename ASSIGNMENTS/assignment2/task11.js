const originalMessage = " Great Service! ";

const cleanedMessage = originalMessage.trim();

console.log(`Original message (with spaces): "${originalMessage}"`);
console.log(`Cleaned message (no spaces): "${cleanedMessage}"`);
