const sales = [2500, 4000, 1800, 3200, 5000];

for(let i=0;i<= sales.length;i++){
    const days = i+1;
    const amount = sales[i];
    console.log(`the sales day ${days} : amount ${amount}`)
}

